---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-be.fullname" . }}
  labels:
    {{- include "agent-be.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "agent-be.config" . | nindent 4 }}
