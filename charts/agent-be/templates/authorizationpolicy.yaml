{{- if .Values.devsecops.AuthorizationPolicy.rules }}
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ include "agent-be.fullname" . }}
  labels:
    {{- include "agent-be.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "agent-be.selectorLabels" . | nindent 6 }}
  action: ALLOW
  rules:
  {{-  tpl (toYaml .Values.devsecops.AuthorizationPolicy.rules) . | nindent 4 }}
{{- end }}
